---
description: 线框图书写规范，适用于所有 wireframe-*.md 文件
globs: docs/wireframe-*.md
alwaysApply: true
---

# 线框图书写规范

## 格式

- 统一使用 **ASCII art** 绘制页面结构，放在 ` ```  ``` ` 代码块内
- 注释用 `←` 标注在行尾，说明交互行为或跳转目标
- 省略号 `· · ·` 表示重复结构，不逐条列出

## 文件命名

`wireframe-{页面名}.md`，例如：
- `wireframe-首页.md`
- `wireframe-话题页.md`
- `wireframe-写文页.md`
- `wireframe-文章详情页.md`

## 必填章节

每个线框图文件必须包含以下章节，顺序固定：

1. **页面结构** — ASCII art 主体
2. **底部 Tab 说明** — 仅在有 Tab 的页面写，表格形式
3. **模块说明** — 逐模块文字说明，包含：数据来源、排序规则、交互行为、跳转目标

## ASCII art 约定

| 元素 | 写法 |
|------|------|
| 页面外框 | `┌─┐ │ └─┘` |
| 模块分隔 | `├─────────────────────────────────┤` |
| 模块标题 | `── 模块N  模块名称 ──` |
| 卡片/图片占位 | `┌────────┐ │ 封面图 │ └────────┘` |
| 按钮 | `[ 按钮文字 → ]` |
| 向右箭头注释 | `← 说明文字`（行尾） |
| 横向滑动 | 注明"横向滑动"字样 |

## 交互描述规范

- 点击行为：`点击 → 跳转 XX页`
- 条件逻辑：`（需登录）`、`（仅认证用户可见）`
- 待定内容：`（排序规则待定）`、`（后端接口决定）`

---

## 讨论文档命名规范

非线框图的过程文档（进度追踪、评审基线、修订追踪等）统一使用：

`{两位序号}-{文档类型}-{简要描述}-{YYYY-MM-DD}.md`

| 类型标识 | 含义 |
|---|---|
| `todo` | 阶段性进度 / 任务清单 |
| `review` | 评审基线（结论冻结后不再改正文） |
| `track` | 修订 / 决策追踪（持续更新） |

序号按创建先后递增（`01`、`02`、`03` …），**只增不减**——新建专题用下一个序号，不因后续新增 `11`、`12` 而替换或覆盖已有文件。

### 项目总入口（TODO 锚点）

**唯一入口文件**：[`docs/09-todo-项目总进度-2026-07-07.md`](docs/09-todo-项目总进度-2026-07-07.md)

- **打开仓库先看此文件**：当前阶段、待办勾选、建议下一步、文档地图均在此维护
- **禁止**再建第二个 `09-todo-…`；进度变化时**原地更新**本文件（改「最后更新」、修订记录、第四节待办勾选）
- 新建 `11-track`、`12-track` 等专题后，在 09-todo **第二节文档地图**补一行链接即可

### 长期驻留锚点 vs 往后生长的分支

| 类型 | 代表文件 | 后续怎么做 |
|---|---|---|
| **锚点·总入口** | `09-todo` | 始终改这一份，序号不变 |
| **锚点·需求准绳** | `10-track-需求文档-v1-…` | 小改原地更新；大版本可新建 `NN-track-需求文档-v2-…`，v1 保留 |
| **锚点·数据表** | `08-track` | 原地更新，表结构迭代写在修订记录 |
| **专题分支** | `05`/`06` 书币/等级，未来 `11` API、`12` 审核… | 一个专题一个文件，**新建序号**，不覆盖 09/10 |

```
09-todo（总控，持续更新）
  ├── 10-track 需求 v1（持续更新）
  ├── 08-track 数据表（持续更新）
  ├── 05 / 06 等专题（持续更新）
  ├── 11-track API（待建 → 新建，不替换 09）
  └── 12-track 审核（待建 → 新建，不替换 10）
```

### 原地更新 vs 新建文件

| 场景 | 做法 |
|---|---|
| 勾掉待办、改阶段说明、补文档地图链接 | **改 09-todo 原文件** |
| 需求细节修订、术语微调 | **改 10-track 原文件** + 修订记录 |
| 新增 API、审核、后台等专题 | **新建** `11-track-…`、`12-track-…`，并在 09-todo 登记 |
| 需求 v1→v2 大改版 | **新建** `NN-track-需求文档-v2-…`；09-todo 标明「当前准绳 = v2」 |
| 某阶段任务全部完成、不再改动 | 该文件标 **已归档**（如 01、03），**不删、不覆盖序号** |

**需求文档**：纳入本命名体系，使用 `track` 类型，如 `10-track-需求文档-v1-2026-07-07.md`；文内保留版本号 v1/v2，文件名保留 `需求文档-vN` 便于识别。

每个文件头部必须紧跟在一级标题下方，包含以下元信息块：

```
> **序号**：NN · **类型**：XX · **创建**：YYYY-MM-DD · **状态**：XXX  
> **上游** → `上游文件名`（触发说明）  
> **下游** → `下游文件名`（产出说明）
```

无上游或下游时该行省略。
